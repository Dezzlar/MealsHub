import { colors } from "./colors";
import { sizes } from "./sizes.js";
import { fontSizes, fontWeights, fonts } from "./fonts.js";
import { lineHeights, space } from "./spacing.js";

export const theme = {
  colors,
  sizes,
  fontSizes,
  fontWeights,
  fonts,
  lineHeights,
  space,
};
